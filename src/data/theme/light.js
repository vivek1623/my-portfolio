import { cyan, purple } from "@mui/material/colors"

const dark = "#1f1f1f"

const theme = {
  palette: {
    primary: {
      main: purple["A400"],
    },
    secondary: {
      main: cyan["400"],
    },
    text: {
      primary: dark,
    },
  },
}

export default theme
