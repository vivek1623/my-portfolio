import { cyan, purple } from "@mui/material/colors"

const theme = {
  palette: {
    primary: {
      main: purple["A400"],
    },
    secondary: {
      main: cyan["400"],
    },
  },
}

export default theme
