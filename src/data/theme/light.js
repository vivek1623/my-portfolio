import { cyan, purple } from "@mui/material/colors"

const dark = "#1f1f1f"

const theme = {
  palette: {
    primary: {
      main: cyan["A400"],
    },
    secondary: {
      main: purple["A700"],
    },
    text: {
      primary: dark,
    },
  },
}

export default theme
