import { cyan, purple, blueGrey } from "@mui/material/colors"

const theme = {
  palette: {
    mode: "dark",
    primary: {
      main: purple["A400"],
    },
    secondary: {
      main: cyan["400"],
    },
    background: {
      default: blueGrey["900"],
    },
  },
}

export default theme
